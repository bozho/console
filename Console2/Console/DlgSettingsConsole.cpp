#include "stdafx.h"
#include "resource.h"

#include "DlgSettingsConsole.h"



LRESULT DlgSettingsConsole::OnInitDialog(UINT /*uMsg*/, WPARAM /*wParam*/, LPARAM /*lParam*/, BOOL& /*bHandled*/) {

	m_brush.CreateSolidBrush(RGB(255, 0, 0));
	return TRUE;
}

//////////////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////////////

LRESULT DlgSettingsConsole::OnCtlColorButton(UINT /*uMsg*/, WPARAM wParam, LPARAM lParam, BOOL& bHandled) {

	CDCHandle dc((HDC)(wParam));

	dc.SetBkColor(RGB(255, 0, 0));

	return (LRESULT)(HBRUSH(m_brush));
/*
	CWindow wnd(GetDlgItem(IDC_DEFAULT_BLACK));
	RECT rect;

	wnd.GetClientRect(&rect);
	CDC dc((HDC)(wParam));

	++rect.left;
	++rect.top;
	--rect.right;
	--rect.bottom;
	dc.FillRect(&rect, m_brush);

	return (LRESULT)(HBRUSH(m_brush));
*/
}

//////////////////////////////////////////////////////////////////////////////


//////////////////////////////////////////////////////////////////////////////

LRESULT DlgSettingsConsole::OnCloseCmd(WORD /*wNotifyCode*/, WORD wID, HWND /*hWndCtl*/, BOOL& /*bHandled*/) {

	EndDialog(wID);
	return 0;
}

//////////////////////////////////////////////////////////////////////////////
